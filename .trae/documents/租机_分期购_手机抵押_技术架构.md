## 1.Architecture design
```mermaid
graph TD
  U["用户浏览器"] --> F["React 前端应用"]
  F --> S["Supabase SDK"]
  S --> B["Supabase 服务（Auth/DB/Storage/Edge Functions）"]

  subgraph "Frontend Layer"
    F
  end

  subgraph "Service Layer (Provided by Supabase)"
    B
  end
```

## 2.Technology Description
- Frontend: React@18 + TypeScript + vite + tailwindcss@3
- Backend: Supabase（Auth + Postgres + Storage + Edge Functions(可选，用于风控规则计算/回调)）

## 3.Route definitions
| Route | Purpose |
|-------|---------|
| / | 首页/业务选择（租机/分期购/手机抵押） |
| /login | 手机号验证码登录/退出 |
| /apply | 统一申请表（根据业务类型切换字段） |
| /orders/:id | 订单详情（状态、补件、签约、支付/放款、还款计划） |
| /admin/reviews | 运营/风控审核台（列表+详情审核） |

## 6.Data model(if applicable)

### 6.1 Data model definition
```mermaid
erDiagram
  PROFILE ||--o{ APPLICATION : has
  APPLICATION ||--o{ ORDER : generates
  ORDER ||--o{ PAYMENT : has
  ORDER ||--o{ REPAYMENT_SCHEDULE : has
  ORDER ||--o{ RISK_REVIEW : has
  DEVICE_ASSET ||--o{ ORDER : referenced_by

  PROFILE {
    uuid user_id
    string phone
    string full_name
    string id_number
    string kyc_status
  }
  APPLICATION {
    uuid id
    uuid user_id
    string biz_type
    string payload_json
    string status
    datetime created_at
  }
  ORDER {
    uuid id
    uuid application_id
    uuid user_id
    string biz_type
    string status
    numeric principal
    numeric deposit
    int term_months
  }
  PAYMENT {
    uuid id
    uuid order_id
    string pay_type
    numeric amount
    string status
    datetime paid_at
  }
  REPAYMENT_SCHEDULE {
    uuid id
    uuid order_id
    int period_no
    date due_date
    numeric amount_due
    numeric amount_paid
    string status
  }
  RISK_REVIEW {
    uuid id
    uuid order_id
    string decision
    string reason_codes
    string reviewer_id
    datetime decided_at
  }
  DEVICE_ASSET {
    uuid id
    string imei
    string model
    string ownership
    string lock_status
  }
```

### 6.2 Data Definition Language
> 说明：为降低早期迭代成本，表间关联采用“逻辑外键”（字段引用）而非物理外键约束。

Profiles（用户扩展信息）
```sql
CREATE TABLE profiles (
  user_id UUID PRIMARY KEY,
  phone VARCHAR(32) NOT NULL,
  full_name VARCHAR(100),
  id_number VARCHAR(64),
  kyc_status VARCHAR(20) DEFAULT 'unverified',
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
GRANT SELECT ON profiles TO anon;
GRANT ALL PRIVILEGES ON profiles TO authenticated;
```

Applications（申请单）
```sql
CREATE TABLE applications (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL,
  biz_type VARCHAR(20) NOT NULL, -- lease/installment/pledge
  payload_json JSONB NOT NULL,
  status VARCHAR(30) NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
GRANT SELECT ON applications TO anon;
GRANT ALL PRIVILEGES ON applications TO authenticated;
```

> 更完整字段与枚举值见《租机_分期购_手机抵押_数据字段字典.md》。