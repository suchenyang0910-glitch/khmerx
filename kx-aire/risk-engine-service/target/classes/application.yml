server:
  port: 8082

spring:
  application:
    name: risk-engine-service
  datasource:
    url: jdbc:postgresql://localhost:5432/kx_aire?currentSchema=risk_engine
    username: khmerx
    password: khmerx
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: localhost
      port: 6379
      database: 1

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true
